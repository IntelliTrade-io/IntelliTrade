Drop or sync the scanner JSON files here for local preview:

- heatmap_pairs_v152.json
- heatmap_currencies_v152.json

The predev and prebuild sync script also searches the parent folder and copies these files here automatically when found.
